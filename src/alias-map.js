const aliasMap = ROOT => ({
  '@src': `${ROOT}src`,
  '@containers': `${ROOT}src/containers`,
  '@components': `${ROOT}src/components`,
  '@css': `${ROOT}src/css/`,
  '@styles': `${ROOT}src/css/`,
  '@config': `${ROOT}config`,
  '@dist': `${ROOT}.dist`,
  '@lib': `${ROOT}lib`,
  '@public': `${ROOT}public`,
  '@test': `${ROOT}test`
});

export default aliasMap;
